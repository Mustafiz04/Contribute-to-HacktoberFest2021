#include <bits/stdc++.h>
using namespace std;
int main()
{
	int t;
	cout<<"Enter the number of testcases";
	cin>>t;
	while(t--)
	{
		int n;
		cout<<"Enter a no. ";
		cin>>n;
		int d = log(n)/log(10)+1;
		int temp = n;
		while(temp != 0)
		{
			
		}
	}
}
